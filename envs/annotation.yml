name: annotation
# Ambiente para Fase 5 — caracterização primária das tripsinas
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - cd-hit>=4.8          # clustering proteico (≥90% aa)
  - diamond>=2.1
  - hmmer>=3.3
  - biopython>=1.83
  - pandas>=2.2
  - numpy>=1.26
  - eggnog-mapper>=2.1   # anotação funcional (usa /databases/eggnog/)
  - seqkit>=2.8
  # SignalP-6.0: instalar via pip após licença acadêmica
  #   pip install signalp-6.0h.fast/  (após download do DTU)
  # InterProScan: opcional, pesado (~12 GB) — usar EggNOG como alternativa
